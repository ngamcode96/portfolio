{% extends 'base.html.twig' %}

{% block stylesheets %} 

{% endblock %}

{% block body %}


    <nav class="navbar">
        <div class="max-width">
            <div class="logo"><a href="#">{{user.firstName}} {{user.lastName}}<span></span></a></div>
            <ul class="menu">
                <li><a href="#home" class="menu-btn li-active">Acceuil</a></li>
                <li><a href="#about" class="menu-btn">A propos</a></li>
                <li><a href="#services" class="menu-btn">Réalisations</a></li>
                <li><a href="#skills" class="menu-btn">Compétences</a></li>
                <!-- <li><a href="#teams" class="menu-btn">Teams</a></li> -->
                <li><a href="#contact" class="menu-btn">Contact</a></li>
            </ul>
            <div class="menu-btn">
                <i class="fas fa-bars"></i>
            </div>
        </div>
    </nav>
    
    {% for message in app.flashes('success') %}
        <div class="alert-success">
            <p>{{ message }} <i class="fas fa-window-close alert_close" id = "alert_close"></i></p>
        </div>
    {% endfor %}
    <section class="home" id="home">
        <div class="max-width">
            <div class="home-content">
                <div class="text-2">{{user.title | raw}}</div>
                 <div class="text-3"><span class="typing"></span></div>
                {# <div class="text-3"><span class=""></span></div> #}

                <div id="image_1" style="display:none">
                    <img src="{{asset('Assets/images/image_1.png')}}" class="my-photo" alt="photo" />
                </div>
                <a href="#contact">Contactez-moi</a> 
            </div>
        </div>


    </section>

    <section class="services" id="services">
            <div class="max-width">

            <h2 class="title">Mes réalisations</h2>
            <div class="serv-content">
                <div class="project-content">
                    <div class="close-project" style="float:right"><i class="far fa-window-close" style="color:#1a2352; font-size:30px; cursor:pointer"></i></div>
                    <div class="project"></div>
                </div>

            {% for realisation in realisations %}
                <div class="card" ref="{{realisation.id}}">
                    <div class="box">
                        {# <i class="fas fa-paint-brush"></i> #}
                        {% if realisation.imageLink %}
                            <img src="{{asset('uploads/images/')}}{{realisation.imageLink}}" style="width:80px" alt="logo - projet" />
                        {% endif %}
                        <div class="text">{{realisation.title}}</div>
                        <p>{{realisation.description|length > 130 ? realisation.description|slice(0, 130) ~ '...' : realisation.description  | raw }}</p>
                        <button class="btn-show" id="btn-show" ref="{{realisation.id}}">Voir les détails</button>

                         
   
 
                    </div>
                </div>    
            {% endfor %}       
            </div>
        </div>
    </section>



    <section class="skills" id="skills">
        <div class="max-width">
            <h2 class="title">Mes compétences</h2>
            <div class="skills-content">
                <div class="column left" style="font-size: 14px">
                    <div class="text">Résumé</div>
                    <p>{{user.resume | raw}}</p>

                </div>
                <div class="column right">
                {% for competence in competences %}
                    <div class="bars">
                        <div class="info">
                            <span>{{competence.name}}</span>
                            <span>{{competence.value}}%</span>
                        </div>
                        <div class="line {{competence.label}}"></div>
                    </div>
                {% endfor %}
                </div>
            </div>
        </div>
    </section>

    <section class="about" id="about">
        <div class="max-width">
            <h2 class="title">A propos de moi</h2>
            <div class="about-content">
                <div class="column left">
                    <img src="{{asset('Assets/images/identity.jpg')}}" alt="">
                </div>
                <div class="column right">
                    <div class="text">Je suis {{user.firstName }} {{user.lastName }} | {{user.age}} ans</div>
                    <p>{{user.description  | raw}}</p>
                    {# <a href="#">Télécharger mon CV</a> #}
                </div>
            </div>
        </div>
    </section>
     <section class="contact" id="contact">
        <div class="max-width">
            <h2 class="title">Contactez-moi</h2>
            <div class="contact-content">
                <div class="column left">
                    {# <div class="text">Get in Touch</div> #}
                    <p>Pour toute informations complémentaires, Veillez laisser un message</p>
                    <div class="icons">
                        <div class="row">
                            <i class="fas fa-user"></i>
                            <div class="info">
                                <div class="head">Nom & Prénom</div>
                                <div class="sub-title">{{user.lastName}} {{user.firstName}}</div>
                            </div>
                        </div>
                        <div class="row">
                            <i class="fas fa-map-marker-alt"></i>
                            <div class="info">
                                <div class="head">Addresse</div>
                                <div class="sub-title">{{user.adress}}</div>
                            </div>
                        </div>
                        <div class="row">
                            <i class="fas fa-envelope"></i>
                            <div class="info">
                                <div class="head">Email</div>
                                <div class="sub-title">{{user.email}}</div>
                            </div>
                        </div>
                         <div class="row">
                            <i class="fas fa-phone"></i>
                            <div class="info">
                                <div class="head">Téléphone</div>
                                <div class="sub-title">{{user.phoneNumber}}</div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="column right">
                    <div class="text">Laissez un message !</div>
                {# {{ form_start(form) }}

                    {{ form_row(form.name, {'attr':{'class':'form-control field'}}) }}
                    {{ form_row(form.sender, {'attr':{'class':'form-control field'}}) }}
                    {{ form_row(form.subject, {'attr':{'class':'form-control field'}}) }}
                    {{ form_row(form.message, {'attr':{'class':'form-control field'}}) }}
            

                    <div style="text-align:center; margin-top:20px"> 
                        <button type="submit" class="btn btn-primary">Enregistrer</button>
                    </div>
                {{ form_end(form) }} #}


                    {{form_start(form)}}
                        
                            {{ form_label(form.name, 'Prénom et NOM: ') }}
                            {{form_widget(form.name,{'attr':{'placeholder': 'Prenom NOM', 'class':'field name'}}) }}
                       
                            {{ form_label(form.sender, 'Adresse E-mail: ') }}
                            {{form_widget(form.sender,{'attr':{'placeholder': 'Adresse E-mail: ', 'class':'field email'}}) }}

                         
                            {{ form_label(form.message, 'Message: ') }}
                            {{form_widget(form.message,{'attr':{'placeholder': 'Message', 'class':'field textarea', 'rows':'10', 'cols': '30'}}) }}
                      
                         <div class="button-area">
                            <button type="submit">Envoyer</button>
                        </div>
                    {{form_end(form)}}
                    {# <form action="#">
                        <div class="fields">
                            <div class="field name">
                                <input type="text" placeholder="Nom et prénom" required>
                            </div>
                            <div class="field email">
                                <input type="email" placeholder="Adresse e-mail" required>
                            </div>
                        </div>
                        <div class="field">
                            <input type="text" placeholder="Objet" required>
                        </div>
                        <div class="field textarea">
                            <textarea cols="30" rows="10" placeholder="Votre message.." required></textarea>
                        </div>
                        <div class="button-area">
                            <button type="submit">Envoyer</button>
                        </div>
                    </form> #}
                </div>
            </div>
        </div>
    </section>

    <div class="scroll-up-btn show">
        <i class="fas fa-angle-up"></i>
    </div>

     <div class="scroll-down-btn show">
        <i class="fas fa-angle-down"></i>
    </div>


{% endblock %}
